import React from "react";

function NavBar() {
  // update the JSX being returned!
  return (
    <div>
      <nav>
        <a href="#home">Home</a>
        <a href="#about">About</a>

      </nav>
    </div>
  );
}

export default NavBar;
